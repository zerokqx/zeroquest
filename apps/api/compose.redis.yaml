services:
  redis:
    image: redis:7
    container_name: zeroquest_redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:${REDIS_PORT:-5102}:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
